# ===========================================
# FIDEITEC - Variables de Entorno Backend
# ===========================================

# App
NODE_ENV=development
PORT=3000

# Database PostgreSQL
DATABASE_URL=postgresql://usuario:password@localhost:5432/fideitec

# JWT (generar con: openssl rand -hex 32)
JWT_SECRET=tu_jwt_secret_aqui_cambiar_en_produccion
JWT_REFRESH_SECRET=tu_refresh_secret_aqui_cambiar_en_produccion
SESSION_SECRET=tu_session_secret_aqui_cambiar_en_produccion
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# Google OAuth (Opcional pero recomendado)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=FIDEITEC <noreply@fideitec.com>

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Landing URL (si es diferente)
LANDING_URL=http://localhost:8080

# Cloudinary (Storage de archivos)
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret
